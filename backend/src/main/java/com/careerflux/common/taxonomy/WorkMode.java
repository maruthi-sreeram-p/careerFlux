package com.careerflux.common.taxonomy;

/** Shared vocabulary: how a role is worked. Used by both job postings and candidate preferences. */
public enum WorkMode {
    ONSITE,
    HYBRID,
    REMOTE,
    UNSPECIFIED
}
