package com.careerflux.candidate.domain;

/** Where the candidate is in the resume-first onboarding flow. */
public enum OnboardingStage {
    RESUME_UPLOAD,
    PROFILE_REVIEW,
    PREFERENCES,
    COMPLETE
}
